const initialState = {
    fontFamily: '"Joti One", cursive',
    fontSize: 80,
    message: `Loading components. Please wait ...`,
  };
  
  function reducer(state = initialState) {
    return state;
  }
  
  export default reducer;